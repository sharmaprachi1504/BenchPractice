pipeline {
    agent any
       stages {
        stage('Creating Infra on AWS') {
            steps {
                bat '''
                cd Bench_Training
                terraform init 
                terraform plan
                terraform validate
                terraform apply --auto-approve
              
                '''
            }
        }    
    }
}
